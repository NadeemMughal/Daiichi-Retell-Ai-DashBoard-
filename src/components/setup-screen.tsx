import Link from "next/link";
import { ArrowRight, Database, KeyRound, ShieldCheck } from "lucide-react";

export function SetupScreen() {
  return <main className="subtle-grid grid min-h-screen place-items-center p-6"><section className="glass w-full max-w-2xl rounded-[28px] p-8 md:p-12"><div className="inline-flex items-center gap-2 rounded-full bg-[#e6f4ed] px-3 py-2 text-xs font-bold uppercase tracking-wider text-[#1f7659]"><ShieldCheck className="size-4" />Secure setup required</div><h1 className="mt-7 text-4xl font-semibold tracking-[-.05em] md:text-5xl">Connect Supabase to activate the portal.</h1><p className="mt-5 max-w-xl leading-7 text-[#687a74]">The application foundation is ready. Authentication and tenant data remain locked until a Supabase project is configured; the portal will not fall back to insecure local authorization.</p><div className="mt-8 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-[#173f3312] bg-white/70 p-5"><Database className="size-5 text-[#1f7659]"/><p className="mt-3 font-semibold">Supabase project</p><p className="mt-1 text-sm text-[#71817c]">Add the project URL, publishable key, database URL, and server secret.</p></div><div className="rounded-2xl border border-[#173f3312] bg-white/70 p-5"><KeyRound className="size-5 text-[#1f7659]"/><p className="mt-3 font-semibold">Server-only Retell</p><p className="mt-1 text-sm text-[#71817c]">The existing Retell key stays on the server and never enters the browser bundle.</p></div></div><Link href="/preview" className="mt-8 inline-flex items-center gap-2 rounded-xl bg-[#164f3e] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#1f7659]">Open design preview <ArrowRight className="size-4" /></Link></section></main>;
}

